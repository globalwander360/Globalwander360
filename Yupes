@Test(expected = DpwEntityNotFoundException.class)
public void linkRequestShouldThrowDpwEntityNotFoundException() {
    // Mock the repository to simulate that the request does NOT exist
    given(requestRepository.findById("1")).willReturn(Optional.empty());

    // Call the method — should throw DpwEntityNotFoundException
    service.linkRequest("30004", "1", "1", "1", "e17938", Boolean.TRUE);
}
